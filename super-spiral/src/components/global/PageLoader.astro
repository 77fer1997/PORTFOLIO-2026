<div class="loader" data-loader aria-hidden="true"><div><span>LC / Software &amp; Diseño</span><strong data-loader-count>00</strong></div><i></i></div>
<style>.loader{position:fixed;inset:0;z-index:1000;background:var(--acid);color:var(--bg);display:flex;flex-direction:column;justify-content:flex-end;padding:var(--pad);transition:transform .7s var(--ease);transform-origin:top}.loader.is-done{transform:scaleY(0);pointer-events:none}.loader>div{display:flex;justify-content:space-between;align-items:end;text-transform:uppercase;font-size:.7rem;letter-spacing:.12em}.loader strong{font-size:clamp(4rem,12vw,10rem);font-weight:400;line-height:.8;letter-spacing:-.07em}.loader i{height:2px;background:var(--bg);margin-top:2rem;transform-origin:left;animation:progress 1.15s var(--ease) both}@keyframes progress{from{transform:scaleX(0)}to{transform:scaleX(1)}}@media(prefers-reduced-motion:reduce){.loader{display:none}}</style>
